def radix_sort(list1):
    max_len = 0
    sorted_temp = list()
    meh_sort = list()
    for i in list1:
        if len(i) > max_len:
            max_len = len(i)
    for i in range(0, max_len):
        for j in list1:
            pass
